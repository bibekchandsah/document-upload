1) Expiring access links (valid for limited time)  (custom minute, hour, day duration)  
2) Smart Search
Search by:
File name
File type
Folder name (implemented)

3) display Creation/modification date (too costly)
4) Create folders inside folders (implemented)
5) detect same file and folder name in the same folder and ask user to rename the file if same file name is detected (implemented)

6) PWA (Installable App)
Turn website into:
Desktop app
Mobile app (implemented)

7) user will upload all files to the github repository integration
8) google document viewer support for all types of files (admin & sharing)
9) github avatar and logout
10) clean print
renaming the file > 1MB fixed
dragging file supported to any folder (sidebar & self sub folders)
show processing icon while deleting, renaming, copying, moving


1. File Management Operations
    - Rename files/folders
    - Move files between folders (drag & drop)
    - Copy files to other folders
    - Delete files/folders with confirmation
    - Bulk operations (select multiple files for delete/move)


- File size warnings before upload (github 100MB)
- Upload progress indicator (like youtube loading)
- QR code for share links
- Lazy loading for large folders
- Revoke share links manually



prompt "
can we add the feature like 
"

one webpage
access
    github token
    it will automatically show github username below it with green checkmark if valid else shows red cross
    user will select github repository name from the list or create new repository (public or private(recommended))
    github branch name (default is main)
    automatically "uploads" folder will be created in the repository
    user will upload files or folders to the "uploads" folder
    
next time login
    it will show github username veryifying from last entered github token
    user will select github repository name from the list or create new repository (by default last selected repository)
    github branch name (default is main)
    show "Login" button if github token is valid





git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/bibekchandsah/document-upload.git
git push -u origin main




git remote add origin https://github.com/bibekchandsah/document-upload.git
git branch -M main
git push -u origin main



git config --global user.email "you@example.com"
git config --global user.name "Your Name"