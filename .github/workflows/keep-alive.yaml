name: Keep Render App Awake

on:
  schedule:
    # - cron: "*/5 * * * *"
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -I https://merodocument.onrender.com
